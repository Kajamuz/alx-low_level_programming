#include "main.h"

/**
 * _abs - Computes the absolute value of the integer 'n'.
 *
 * @n: The integer value to compute the absolute value of.
 *
 * Return: The absolute value of 'n'.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
