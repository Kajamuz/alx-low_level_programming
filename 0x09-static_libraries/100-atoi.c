#include <stdlib.h>

/**
 * _atoi - Converts string 's' to an integer representation.
 *
 * @s: Pointer to the string to be converted.
 *
 * Return: The converted integer value of 's'.
 */
int _atoi(char *s)
{
	return (atoi(s));
}
